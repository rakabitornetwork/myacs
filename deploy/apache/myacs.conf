# MyACS di belakang Apache (myacs.teslatech.my.id)
# Butuh: a2enmod proxy proxy_http proxy_wstunnel ssl headers rewrite
# Salin ke: /etc/apache2/sites-available/myacs.conf
#
# PENTING: GenieACS UI sudah memakai port 3000 — MyACS harus di port 3001

<VirtualHost *:80>
    ServerName myacs.teslatech.my.id
    RewriteEngine On
    RewriteRule ^ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]
</VirtualHost>

<IfModule mod_ssl.c>
<VirtualHost *:443>
    ServerName myacs.teslatech.my.id

    SSLEngine on
    SSLCertificateFile    /etc/letsencrypt/live/myacs.teslatech.my.id/fullchain.pem
    SSLCertificateKeyFile /etc/letsencrypt/live/myacs.teslatech.my.id/privkey.pem
    Include /etc/letsencrypt/options-ssl-apache.conf

    ProxyPreserveHost On
    RequestHeader set X-Forwarded-Proto "https"
    RequestHeader set X-Forwarded-For "%{REMOTE_ADDR}s"

    # Upload firmware / file CPE — hingga 100MB
    LimitRequestBody 104857600

  # MyACS Node.js (bukan GenieACS di :3000)
    ProxyPass        / http://127.0.0.1:3001/
    ProxyPassReverse / http://127.0.0.1:3001/

    ErrorLog ${APACHE_LOG_DIR}/myacs-error.log
    CustomLog ${APACHE_LOG_DIR}/myacs-access.log combined
</VirtualHost>
</IfModule>
